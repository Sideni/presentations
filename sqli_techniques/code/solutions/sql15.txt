Soustraction : 

b - a :

SELECT (TIMESTAMPDIFF (SECOND, FROM_UNIXTIME (a),FROM_UNIXTIME (b)));


Addition :

a + b

SELECT UNIX_TIMESTAMP (TIMESTAMPADD (SECOND, a,FROM_UNIXTIME (b)));


(SELECT UNIX_TIMESTAMP (TIMESTAMPADD (SECOND, a,FROM_UNIXTIME (b))))
(SELECT (TIMESTAMPDIFF (SECOND, FROM_UNIXTIME (a),FROM_UNIXTIME (b))))

